### file organizer
this is a simple bash script that organizes files in the current directory into specific folders based on their file extensions.

it moves images, documents, and media into dedicated folders and anything else is put inside 'Others', it does not touch any directories

### to run the script

simply download the script file and run it this way:

``` bash organize.sh ```

enjoy your neat directory!